{
  "name": "portalpair",
  "version": "1.0.0",
  "scripts": {
    "start": "env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy -u ALL_PROXY -u all_proxy node server.js",
    "serve": "npx serve -l 8080",
    "expand-seasons": "env -u HTTP_PROXY -u HTTPS_PROXY HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 .venv/bin/python -m scripts.expand_seasons"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  }
}
